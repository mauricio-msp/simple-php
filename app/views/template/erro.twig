<!DOCTYPE html>

<html lang="pt-br">
    <head>
        <title> {{ simple['title'] }}  </title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width = device-width, initial-scale =1.0, maximum-scale = 1.0, user-scalable = no" />
        
        <!-- Favicon -->
        <link rel="shorcut icon" href="{{ simple['asset'] }}/img/icone - not-found.png" />
        
        <!-- Components -->
        <link rel="stylesheet" type="text/css" href="{{ simple['asset'] }}/bower_components/bootstrap/css/bootstrap.min.css" />
        <link rel="stylesheet" type="text/css" href="{{ simple['asset'] }}/bower_components/font-awesome/css/font-awesome.min.css" />
        
        <!-- Fonts -->
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Raleway" />
        
        <!-- Css -->
        <style type="text/css">
            html, body {
                font-family: 'Raleway', sans-serif;
            }

            .container {
                margin-top: 30px;
            }

            .row {
                padding: 30px;
            }

            .col-lg-8 {
                margin: 0 auto;
                float: none;
            }

            .btn {
                background-color: #0F0050;
                border: 1px solid #0F0050;
                color: #FFF;

                transition: all ease-in-out 0.2s;
            }

            .btn:hover,
            .btn:focus {
                background-color: #FFF;
                border: 1px solid #0F0050;
                color: #0F0050;
            }
        </style>
    </head>
    <body>
        
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="jumbotron">
                        <h1 class="text-center"> <i class="fa fa-frown-o"></i> !Oops </h1>
                        <p class="text-center"> 
                            Página não encontrada <br /> 
                            <span> A url requisitada ou o endereço acessado não existe. <br /> Por favor, volte ao inicio.</span>
                        </p>
                        
                        <p class="text-center">
                            <a class="btn btn-default btn-lg" href="{{ simple['base'] }}" role="button">
                                <i class="fa fa-arrow-left"></i> Voltar ao inicio
                            </a>
                        </p>
                    </div>
                    
                    <hr />
                    
                    <p class="text-center"> 
                        Copyright <i class="fa fa-copyright"></i> {{ simple['data'] }} - Todos os direitos reservados <br />
                        <span class="text-muted"> Designed by Maurício Porfírio </span>
                    </p>
                </div>
            </div>
        </div>
        
    </body>
</html>


